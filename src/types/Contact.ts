// src/types/Contact.ts
export interface Contact {
    _id: string; // Assuming _id is a string. Change this if it's a different type
    name: string;
    phone: string;
    email: string;
    address: string;
    jobTitle: string;
    company: string;
    dob: string;
  }
  